# PostgreSQL connection for the `agency` database.
# Keep real credentials in .env.local and never commit that file.
PGHOST=localhost
PGPORT=5432
PGDATABASE=agency
PGUSER=postgres
PGPASSWORD=dali2002

